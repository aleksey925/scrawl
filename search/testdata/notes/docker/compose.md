Docker Compose
==============

Пример файла описания сервисов:

```yaml
services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
```

Образ nginx собирается автоматически при первом запуске.
Один файл описывает сразу все сервисы приложения.
