оказывает все равно влияение на порядок загрузки и что есть 2 прохода)

```toml
[[env]]
_.file = ".env"

[[env]]
_.source = "script.sh"

[[env]]
_.python.venv = { path = ".venv", create = true }
```
---

## Как хранить секреты в проектах: sops and age

Секреты проекта лежат рядом с кодом в зашифрованном файле, а mise
